[env]
# BuildKit repository configuration
BUILDKIT_REPO = "https://github.com/moby/buildkit.git"
BUILDKIT_REF = "v0.25.2"

# GoogleAPIs repository configuration  
GOOGLEAPIS_REPO = "https://github.com/googleapis/googleapis.git"
GOOGLEAPIS_REF = "master"

# Build configuration
PROTO_DIR = "proto"
PROTO_REBUILD = "false"
## "content" (default, download directly) or "clone" (clone repository)
PROTO_FETCH_MODE = "content"
